# HA-RAG-Bridge Testing Configuration
# For automated testing and CI/CD

# =============================================================================
# HOME ASSISTANT CONNECTION (Mock/Test)
# =============================================================================
HA_URL=http://localhost:8123
HA_TOKEN=test-token-placeholder
ADMIN_TOKEN=test-admin-token

# =============================================================================
# ARANGODB DATABASE (Test)
# =============================================================================
ARANGO_URL=http://localhost:8529
ARANGO_DB=homeassistant_test
ARANGO_USER=root
ARANGO_PASS=test123

# =============================================================================
# EMBEDDING CONFIGURATION (Minimal for testing)
# =============================================================================
EMBEDDING_BACKEND=local
SENTENCE_TRANSFORMER_MODEL=paraphrase-multilingual-mpnet-base-v2
EMBED_DIM=384

# External API keys (fake for testing)
OPENAI_API_KEY=test-openai-key
GEMINI_API_KEY=test-gemini-key
GEMINI_BASE_URL=https://generativelanguage.googleapis.com
GEMINI_OUTPUT_DIM=768

# =============================================================================
# QUERY PROCESSING & CONVERSATION (Fast for testing)
# =============================================================================
# Query Rewriting (fast timeouts)
QUERY_REWRITING_ENABLED=false
QUERY_REWRITING_MODEL=disabled
QUERY_REWRITING_API_BASE=http://localhost:8001/v1
QUERY_REWRITING_API_KEY=fake-key
QUERY_REWRITING_TIMEOUT_MS=100
COREFERENCE_RESOLUTION_ENABLED=false

# Conversation Summarization (disabled for speed)
CONVERSATION_SUMMARY_ENABLED=false
CONVERSATION_SUMMARY_MODEL=disabled
CONVERSATION_SUMMARY_TIMEOUT=1000
CONVERSATION_SUMMARY_API_BASE=http://localhost:8001/v1
CONVERSATION_SUMMARY_API_KEY=fake-key

# Memory Settings (minimal)
MEMORY_TOPIC_BOOST_ENABLED=false
MEMORY_DECAY_CONSTANT=60

# =============================================================================
# EXTERNAL INTEGRATIONS (Disabled)
# =============================================================================
INFLUX_URL=
INFLUX_TOKEN=
INFLUX_ORG=test
INFLUX_BUCKET=test
INFLUX_DB=test
INFLUX_USER=test
INFLUX_PASS=
INFLUX_MEASUREMENT=

# =============================================================================
# TESTING SETTINGS
# =============================================================================
LOG_LEVEL=WARNING
DEBUG=false
LOG_FILE=

# Pipeline Tracing (disabled for speed)
TRACE_DETAIL_LEVEL=none
TRACE_META_TASKS=false

# Cross Encoder Model (lightweight)
CROSS_ENCODER_MODEL=cross-encoder/ms-marco-MiniLM-L-6-v2