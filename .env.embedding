# Local Embedding Model Configuration
# 
# Available models (in order of preference for your hardware):
# 1. paraphrase-multilingual-MiniLM-L12-v2  - Best for Hungarian+English (384 dim)
# 2. all-MiniLM-L12-v2                      - Better than L6, still fast (384 dim)  
# 3. all-mpnet-base-v2                      - High quality, slower (768 dim)
# 4. all-MiniLM-L6-v2                       - Current default, fastest (384 dim)

SENTENCE_TRANSFORMER_MODEL=paraphrase-multilingual-MiniLM-L12-v2

# If you want higher quality embeddings and don't mind slower processing:
# SENTENCE_TRANSFORMER_MODEL=all-mpnet-base-v2

# For the fastest option (current):
# SENTENCE_TRANSFORMER_MODEL=all-MiniLM-L6-v2
